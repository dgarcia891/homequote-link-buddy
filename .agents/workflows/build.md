---
name: build
description: "Trigger parallel swarm for logic verification and implementation"
---
1. **TRIGGER PARALLEL SWARM.** 
2. Spawn "Test Writer Agent" for logic verification. 
3. Spawn "Developer Agent" for implementation (`src/` or `supabase/functions/`).
4. Ensure both run simultaneously.
