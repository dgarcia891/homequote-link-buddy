import type { LeadInsert } from "@/types";

/**
 * STUB: Lead scoring service.
 * Returns a default score. Replace with real scoring logic later.
 */
export function scoreLead(_lead: LeadInsert): number {
  return 50; // default score
}
